git init
git add README.md (para subir todo va elpunto)
git commit -m "first commit" (aqui se agregar el coemntario)
git branch -M main
git remote add origin https://github.com/PoshuaDev-hub/ICIFG003-EQ14.git
git push -u origin main (Aqui se sube al origen)